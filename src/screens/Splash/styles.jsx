import { StyleSheet } from 'react-native';
import { COLORS } from '../../common';

export default StyleSheet.create({
    img:{
        width: "100%", 
        height: "100%",
        flex:1
    },

});
